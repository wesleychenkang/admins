<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaoqu.dao.MenuDao">
    <select id="getAllMenus" resultType="com.miaoqu.entity.Menu">
        select tid, name, createTime, parentID, path,createTime
        from menu
    </select>
</mapper>
